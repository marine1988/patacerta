# PataCerta — robots.txt
# Produção: patacerta.pt | Staging: stage.patacerta.pt
# (sem esquema de propósito: este ficheiro vai para a imagem de todos os
#  ambientes e o guard do build falha se encontrar um host do produto que não
#  seja o do ambiente — ver plugin `patacerta:public-url` no vite.config.ts)
# Documentação: https://www.robotstxt.org/

# ============================================================
# Crawlers de pesquisa generalistas (Google, Bing, DuckDuckGo, ...)
# ============================================================
User-agent: *
Allow: /
Disallow: /admin
Disallow: /area-pessoal
Disallow: /painel
Disallow: /publicar
Disallow: /onboarding
Disallow: /verificar-email
Disallow: /recuperar-palavra-passe
Disallow: /redefinir-password
# Rotas legacy que fazem 302 client-side (não indexar; preferir o canónico)
Disallow: /diretorio
Disallow: /servicos$
Disallow: /mapa
Disallow: /explorar

# ============================================================
# LLMs / AI training & retrieval
# ============================================================
# Política: PERMITIR retrieval (browsing em tempo real) para que
# ChatGPT/Claude/Perplexity/Gemini possam citar a PataCerta como
# fonte. NÃO permitir uso para treino sem opt-in explícito.
#
# Notas:
# - Google-Extended controla treino do Gemini; Googlebot continua a
#   indexar normalmente para Search.
# - GPTBot é o crawler de treino da OpenAI; ChatGPT-User / OAI-SearchBot
#   são os de browsing em tempo real.
# - ClaudeBot é o crawler da Anthropic (treino + busca).
# - PerplexityBot indexa para responder a queries em tempo real.
# - CCBot é o Common Crawl, fonte de muitos datasets de treino.

# OpenAI — bloquear treino
User-agent: GPTBot
Disallow: /

# OpenAI — permitir browsing/retrieval
User-agent: ChatGPT-User
Allow: /
Disallow: /admin
Disallow: /area-pessoal
Disallow: /painel
Disallow: /publicar
Disallow: /onboarding

User-agent: OAI-SearchBot
Allow: /
Disallow: /admin
Disallow: /area-pessoal
Disallow: /painel
Disallow: /publicar
Disallow: /onboarding

# Anthropic
User-agent: ClaudeBot
Allow: /
Disallow: /admin
Disallow: /area-pessoal
Disallow: /painel
Disallow: /publicar
Disallow: /onboarding

User-agent: anthropic-ai
Disallow: /

# Google — Gemini training
User-agent: Google-Extended
Disallow: /

# Perplexity
User-agent: PerplexityBot
Allow: /
Disallow: /admin
Disallow: /area-pessoal
Disallow: /painel
Disallow: /publicar
Disallow: /onboarding

# Common Crawl (usado em datasets de treino) — bloqueado
User-agent: CCBot
Disallow: /

# Bytedance / Apple / Meta — bloquear treino
User-agent: Bytespider
Disallow: /

User-agent: Applebot-Extended
Disallow: /

User-agent: FacebookBot
Disallow: /

User-agent: Meta-ExternalAgent
Disallow: /

# ============================================================
# Sitemap
# ============================================================
#
# O dominio abaixo NAO e' literal: e' resolvido no build pelo ambiente onde
# corre (build-arg `VITE_PUBLIC_URL` — a mesma fonte do `<link rel=canonical>`
# e das `<loc>` do sitemap; ver src/lib/seo.ts). Em dev/preview quem resolve
# e' o plugin `patacerta:public-url` (vite.config.ts), que trata tambem do
# `llms.txt` (mesma classe de defeito — PATA-BUG-9 aqui, PATA-BUG-10 la).
#
# NUNCA escrever aqui um host literal: este ficheiro e' estatico, e' copiado
# tal-e-qual para a imagem de TODOS os ambientes, logo um host fixo faz o
# stage anunciar a sitemap de producao (defeito PATA-BUG-9).
#
Sitemap: __PUBLIC_URL__/sitemap.xml
